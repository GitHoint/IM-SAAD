<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Catalogue :: AML</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="/css/main.css">
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.7.1/jquery.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
</head>
<body>
    <header class="shadow">
        <nav class="navbar navbar-expand-lg navbar-custom bg-custom">
            <div class="container-fluid">
                <a class="navbar-brand" href="#">AML</a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarNav">
                    <ul class="navbar-nav">
                        <li class="nav-item active">
                            <a class="nav-link" href="#">Home</a>
                        </li>
                    </ul>
                    <form class="form-inline d-flex" method="post" action="catalogue">
                        <input class="form-control mr-sm-2" type="search" name="query" placeholder="Browse The Catalogue" aria-label="Search">
                        <button class="btn btn-outline-success my-2 my-sm-0" type="submit">Search</button>
                      </form>
                    <ul class="navbar-nav ms-auto">
                        <li class="nav-item">
                            <a class="nav-link" href="#"><i class="bi bi-person"></i> Sign Up</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/login"><i class="bi bi-box-arrow-in-right"></i> Login</a>
                        </li>
                    </ul>
                </div>
            </div>
        </nav>
    </header>
    <main class="searchContainer">
        <% if(searchResults.length > 0){ %>
            <% for (let i=0; i<searchResults.length; i++){ %>
                <div class="card">
                    <form action="/borrow" method="post">
                        <input type="hidden" name="mediaId" value="<%= searchResults[i].mediaId %>">
                        <div class="card-header text-center"><%=searchResults[i].name%></div>
                        <div class="card-body d-flex justify-content-between"><%=searchResults[i].description%>
                            <button type="submit" class="btn btn-outline-success">Borrow</button>
                        </div>
                    </form>
                </div>
            <% } %>
        <% } else { %>
            <p>No Avaliable Results</p>
            <% } %>
        
    </main>
    <footer class="footer">
        <p>AML Library System - 2024©</p>
      </footer>
</body>
</html>