<header>
    <nav class="navbar navbar-expand-lg navbar-custom bg-custom">
        <div class="container-fluid">
            <a class="navbar-brand" href="#">AML</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav">
                    <li class="nav-item active">
                        <a class="nav-link" href="/home">Home</a>
                    </li>
                    <% if (currUser.type == "member") { %>
                        <li class="nav-item active">
                            <a class="nav-link" href="/returnSearch">Return Media</a>
                        </li>
                    <% } if (currUser.type == "staff") { %>
                        <li class="nav-item active">
                            <a class="nav-link" href="/procurement">Add Stock</a>
                        </li>
                     <% } %>
                </ul>
                <form class="form-inline d-flex" method="post" action="/search">
                    <% if (currUser.type == "member" || currUser.type == "staff") { %>
                        <input class="form-control mr-sm-2" type="search" name="query" placeholder="Browse The Catalogue" aria-label="Search">
                        <button class="btn btn-outline-success my-2 my-sm-0" type="submit">Search</button>
                    <% } %>
                  </form>
                <ul class="navbar-nav ms-auto">
                    <% if (currUser.type == null) { %>
                        <li class="nav-item">
                            <a class="nav-link" href="/register"><i class="bi bi-person"></i> Sign Up</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/login"><i class="bi bi-box-arrow-in-right"></i> Login</a>
                        </li>
                    <% } else { %>
                        <li class="nav-item">
                            <a class="nav-link" href="/account-page"><i class="bi bi-box-arrow-in-right"></i> Manage Account</a>
                        </li>
                        <li class="nav-item">
                            <form action="/logout" method="post">
                                <button class="btn btn-light" type="sumbit">Logout</button>
                            </form>
                        </li>
                    <% } %>
                </ul>
            </div>
        </div>
    </nav>
</header>